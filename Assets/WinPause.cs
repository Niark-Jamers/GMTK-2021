using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class WinPause : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    public void WinPause2()
    {
        Time.timeScale = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
